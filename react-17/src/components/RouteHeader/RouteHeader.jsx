import React  from 'react';

import './RouteHeader.scss';

const RouterHeader = ({ categoryName, path }) => (<div data-testid="route-header" className="route-header" />);

export default RouterHeader;

