import React  from 'react';

import './Tracks.scss';

const Tracks = ({ categoryName, data, isLoading, path }) => (<div data-testid="tracks" className="tracks" data-testid="tracks" />);

export default Tracks;

