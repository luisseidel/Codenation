import React  from 'react';

import './Playlists.scss';

const Playlists = ({ data, categoryName, categoryId, isLoading, path }) => (<div data-testid="playlists" className="playlists" />);

export default Playlists;

